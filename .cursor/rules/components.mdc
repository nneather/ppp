---
description: Reusable component inventory and patterns. Use these, do not reinvent.
globs: src/lib/components/**,src/routes/**/*.svelte
alwaysApply: false
---

# Component inventory — reuse before creating

If you reach for one of these capabilities, use the existing component.

| Capability | Component |
|---|---|
| Branded full-page error boundary (404 / 403 / 500) — status-aware title + message + "Go to dashboard" | [src/routes/+error.svelte](mdc:src/routes/+error.svelte) |
| Multi-value text/email input with chips | [src/lib/components/email-chips-editor.svelte](mdc:src/lib/components/email-chips-editor.svelte) |
| Destructive confirm with typed message | [src/lib/components/confirm-dialog.svelte](mdc:src/lib/components/confirm-dialog.svelte) |
| Entity create/edit form in a sheet | [src/lib/components/client-form-sheet.svelte](mdc:src/lib/components/client-form-sheet.svelte) |
| Child-record (effective-dated) form sheet | [src/lib/components/rate-form-sheet.svelte](mdc:src/lib/components/rate-form-sheet.svelte) |
| Global setting modal | [src/lib/components/default-cc-dialog.svelte](mdc:src/lib/components/default-cc-dialog.svelte) |
| Single sheet shared by create + edit | [src/lib/components/time-entry-sheet.svelte](mdc:src/lib/components/time-entry-sheet.svelte) |
| "Generate derived record" wizard | [src/lib/components/generate-invoice-sheet.svelte](mdc:src/lib/components/generate-invoice-sheet.svelte) |
| Audit log row with collapsible JSONB diff | [src/lib/components/audit-row.svelte](mdc:src/lib/components/audit-row.svelte) |
| Book entity form (create + edit) shell — identity, classification, state, series, **citation abbreviation** (overrides series abbr — [125](mdc:docs/decisions/125-books-citation-abbreviation.md)), save bar; hosts `<BookFormAuthors>` + `<BookFormPublication>`; **In physical library** (`owned`) checkbox ([103](mdc:docs/decisions/103-library-not-owned-session-1.md)) | [src/lib/components/book-form.svelte](mdc:src/lib/components/book-form.svelte) |
| 1–5 star rating control (`books.rating`); hidden input for forms; optional `onchange` for detail-page auto-save | [src/lib/components/book-rating-scale.svelte](mdc:src/lib/components/book-rating-scale.svelte) |
| Book form author junction rows + B14 dedup + inline person create/edit dialog (edit = same `person_id` via `?/updatePerson` — [143](mdc:docs/decisions/143-ol-edition-prefill-person-edit.md)) | [src/lib/components/book-form-authors.svelte](mdc:src/lib/components/book-form-authors.svelte) |
| Book form publication fields — `variant="essentials"` (publisher/year) or `variant="extended"` (edition/reprint/ISBN); `bind:pub` | [src/lib/components/book-form-publication.svelte](mdc:src/lib/components/book-form-publication.svelte) |
| Scripture mobile Bible book picker sheet | [src/lib/components/scripture-bible-picker-sheet.svelte](mdc:src/lib/components/scripture-bible-picker-sheet.svelte) |
| Open Library ISBN refresh (camera + manual fetch, field pickers) — edit-mode metadata merge; **dynamic-imported** from `<BookForm>` when the dialog opens (keeps `@zxing/*` out of new-book + edit base chunks) | [src/lib/components/book-ol-refresh-dialog.svelte](mdc:src/lib/components/book-ol-refresh-dialog.svelte) |
| Polymorphic `(book_id OR essay_id)` source picker — essay branch gated behind `FEATURE_ESSAYS_UI` in `polymorphic.ts` (polymorphic child tables only; parent-book essays UI ships via `<BookEssaysEditor>`) | [src/lib/components/source-picker.svelte](mdc:src/lib/components/source-picker.svelte) |
| Embedded scripture-reference sub-form shell — batch/edit modes; hosts `<ScriptureOcrQueue>` + `<ScriptureRowEditor>`; edit-mode image upload; host page handles soft-delete via `<ConfirmDialog>` | [src/lib/components/scripture-reference-form.svelte](mdc:src/lib/components/scripture-reference-form.svelte) |
| Scripture batch OCR queue — gallery/camera/PDF pick, parallel pipeline, page status chips, merge into draft rows | [src/lib/components/scripture-ocr-queue.svelte](mdc:src/lib/components/scripture-ocr-queue.svelte) |
| Single scripture draft row editor — compact strip + expanded mobile/desktop layouts | [src/lib/components/scripture-row-editor.svelte](mdc:src/lib/components/scripture-row-editor.svelte) |
| Typeahead person picker with B14 dedup warnings + inline create; optional `initialQuery` + `seedKey` to seed OL author hints; `autoOpenOnSeed` to open the list only for the first unresolved row | [src/lib/components/person-autocomplete.svelte](mdc:src/lib/components/person-autocomplete.svelte) |
| Edit-person dialog (name fields + B14 collision confirm + optional View books link); posts `?/updatePerson` — book detail author tap ([158](mdc:docs/decisions/158-book-detail-polish-author-edit.md)); **create mode** when `person` is null (`?/createPerson` + `prefill`) for essay author inline create ([162](mdc:docs/decisions/162-essay-author-inline-create.md)) | [src/lib/components/person-edit-dialog.svelte](mdc:src/lib/components/person-edit-dialog.svelte) |
| Canonical-name + abbreviations typeahead with optional inline create + optional client-side typo warning (used for `ancient_texts` + `book_topics.topic`) | [src/lib/components/canonicalizing-combobox.svelte](mdc:src/lib/components/canonicalizing-combobox.svelte) |
| Multi-select typeahead facet (Series + Author + Bible book coverage filters on `/library`); chips above an input, keywords-aware filter, no create path | [src/lib/components/multi-combobox.svelte](mdc:src/lib/components/multi-combobox.svelte) |
| Batch-capable topic entry form on `/library/books/[id]` — mirrors `<ScriptureReferenceForm>`; wraps `<SourcePicker>` + `<CanonicalizingCombobox>` for the topic field | [src/lib/components/book-topic-form.svelte](mdc:src/lib/components/book-topic-form.svelte) |
| Essays & articles CRUD on book detail (`reference_work` + `edited_volume` parents) — section open by default; `#essay-<uuid>` deep-link highlight; **batch create** (Add/Duplicate/Remove rows → `?/createEssaysBatch`); single-row edit; per-row Copy Footnote/Bibliography; owner-gated write; **inline person create** via `<PersonAutocomplete onCreate>` → `<PersonEditDialog>` ([162](mdc:docs/decisions/162-essay-author-inline-create.md)) | [src/lib/components/book-essays-editor.svelte](mdc:src/lib/components/book-essays-editor.svelte) |
| 66-chip Bible-book coverage toggle for `book_bible_coverage` (hard-delete on untoggle; UNIQUE(book_id, bible_book) makes re-add idempotent; invalidates `app:library:book:<id>` not `invalidateAll`) | [src/lib/components/book-bible-coverage-editor.svelte](mdc:src/lib/components/book-bible-coverage-editor.svelte) |
| Ancient-text coverage picker + chip list for `book_ancient_coverage`; owner-only inline `ancient_texts` create via combobox `onCreate`; book-scoped invalidate (+ `ancient_texts` on create) | [src/lib/components/book-ancient-coverage-editor.svelte](mdc:src/lib/components/book-ancient-coverage-editor.svelte) |
| Underline-the-mnemonic-letter label helper (used inside `<Button hotkey>` and standalone) | [src/lib/components/hotkey-label.svelte](mdc:src/lib/components/hotkey-label.svelte) |
| Page title + optional back link + `lead` / `eyebrow` / `meta` / `actions` snippets (mobile-stacked actions, `break-words` title); pair with `pb-tabbar` / `bottom-tabbar` in `app.css` for the mobile tab bar | [src/lib/components/page-header.svelte](mdc:src/lib/components/page-header.svelte) |
| PWA new-version toast (`virtual:pwa-register/svelte`); on resume after background, auto-applies if `registration.waiting` or `needRefresh` when idle ([082](mdc:docs/decisions/082-pwa-update-auto-recover.md)/[111](mdc:docs/decisions/111-pwa-resume-auto-apply-harden.md)); **defers** silent reload when `isUserBusy()` (open sheet/dialog or focused editable) — desktop tab return + mobile ([186](mdc:docs/decisions/186-pwa-defer-reload-while-editing.md)); mid-session **top** banner with **Update now** + Later; mounted from root layout via dynamic import | [src/lib/components/PwaReloadToast.svelte](mdc:src/lib/components/PwaReloadToast.svelte) |
| Module skeleton shown while `navigating` (tab-switch perceived speed; see performance.mdc) | [src/lib/components/nav-module-skeleton.svelte](mdc:src/lib/components/nav-module-skeleton.svelte) |
| Turabian footnote/bibliography preview block with italic HTML segments + HTML+plain Copy | [src/lib/components/turabian-citation-block.svelte](mdc:src/lib/components/turabian-citation-block.svelte) |
| Review deck picker rail (label + hint + live count cards; URL routing owned by host) | [src/lib/components/review-deck-picker.svelte](mdc:src/lib/components/review-deck-picker.svelte) |
| Review header HUD: today/session counts, live burndown bar, sprint progress ring | [src/lib/components/review-sprint-hud.svelte](mdc:src/lib/components/review-sprint-hud.svelte) |
| Genre Sprint chip row (top genres + More…, AI-suggested chip with sparkle; one tap = confirm via `onPick`) | [src/lib/components/review-genre-chips.svelte](mdc:src/lib/components/review-genre-chips.svelte) |
| One-time milestone interstitial banner (positive framing, dismissible) | [src/lib/components/review-milestone-card.svelte](mdc:src/lib/components/review-milestone-card.svelte) |
| End-of-sprint summary card (cleared/skipped/elapsed + go-again CTA) | [src/lib/components/review-sprint-summary.svelte](mdc:src/lib/components/review-sprint-summary.svelte) |
| AI research proposal panel on review cards (per-field current→proposed, Apply/Apply all fill quick-edit state, Dismiss rejects) | [src/lib/components/review-proposal-panel.svelte](mdc:src/lib/components/review-proposal-panel.svelte) |
| Dashboard library tile footer — needs-review link when count > 0 | [src/lib/components/dashboard-library-tile-footer.svelte](mdc:src/lib/components/dashboard-library-tile-footer.svelte) |
| Dashboard upcoming sermons panel (`preached_on >= today`, venue/topic/passage) | [src/lib/components/dashboard-upcoming-sermons.svelte](mdc:src/lib/components/dashboard-upcoming-sermons.svelte) |
| Dashboard classwork due-soon panel (14d open assignments, overdue first; desktop Now column) | [src/lib/components/dashboard-due-soon.svelte](mdc:src/lib/components/dashboard-due-soon.svelte) |
| Dashboard contacts due-to-meet panel (cadence overdue / never logged; desktop Now column) | [src/lib/components/dashboard-contacts-due.svelte](mdc:src/lib/components/dashboard-contacts-due.svelte) |
| Dashboard invoicing tile "Generate last week's invoice" footer (weekly clients with unbilled prior week) | [src/lib/components/dashboard-invoicing-tile-footer.svelte](mdc:src/lib/components/dashboard-invoicing-tile-footer.svelte) |
| Inline project tree + weekly 5-segment health picker (carry-forward seed uses `untrack` in `$effect`; optional URL filters + latest-health trend badge; domain color dot + child left rail) | [src/lib/components/project-tree.svelte](mdc:src/lib/components/project-tree.svelte) |
| Project metadata create/edit Sheet (~6 fields + Deferred until; cycle-safe parent picker) | [src/lib/components/project-form-sheet.svelte](mdc:src/lib/components/project-form-sheet.svelte) |
| Domain color swatch dialog (`?/setProjectColor`) for root projects | [src/lib/components/project-color-picker.svelte](mdc:src/lib/components/project-color-picker.svelte) |
| Epic PR health shape icon (circle / half-circle / inverted triangle / diamonds) | [src/lib/components/health-status-icon.svelte](mdc:src/lib/components/health-status-icon.svelte) |
| Week-over-week trend triangles (green up / red down) | [src/lib/components/health-trend-arrow.svelte](mdc:src/lib/components/health-trend-arrow.svelte) |
| Health shape + label + trend (`showStatusIcon` to hide shape on `/projects` tree) | [src/lib/components/health-trend-badge.svelte](mdc:src/lib/components/health-trend-badge.svelte) |
| Read-only dashboard domain status strip (expand children, deep-link domains; domain color dots) | [src/lib/components/project-status-strip.svelte](mdc:src/lib/components/project-status-strip.svelte) |
| URL-synced lifecycle / health / domain filter bar for `/projects` (collapsed by default; `bind:open`; desktop summary; mobile trigger on page week row) | [src/lib/components/project-filter-bar.svelte](mdc:src/lib/components/project-filter-bar.svelte) |
| MYN zoned task list (Critical / Opportunity / OTH; Critical + Target Now underline; one-step raise zone; defer presets + next-Monday OTH; promote/complete; notes; recurring delete This/Entire series; `compact` for dashboard Now pane) | [src/lib/components/project-task-list.svelte](mdc:src/lib/components/project-task-list.svelte) |
| MYN task create/edit Sheet (project, zone, start date, notes; Outlook-like weekly/monthly recurrence; sticky save bar; series scope dialog) | [src/lib/components/project-task-sheet.svelte](mdc:src/lib/components/project-task-sheet.svelte) |
| Sermon create/edit Sheet (date, venue, context C/P/A, topic, passage display + structured rows, notes; inline venue create) | [src/lib/components/sermon-form-sheet.svelte](mdc:src/lib/components/sermon-form-sheet.svelte) |
| Course create/edit Sheet (name, code/instructor/term, status, Education-suggested `project_id`, notes) | [src/lib/components/course-form-sheet.svelte](mdc:src/lib/components/course-form-sheet.svelte) |
| Contact create/edit Sheet (name, email/phone, household picker, optional mailing → household-of-one, months/years meet cadence, status, no_reminders, list toggles) | [src/lib/components/contact-form-sheet.svelte](mdc:src/lib/components/contact-form-sheet.svelte) |
| Meet cadence amount + months/years unit fields (profile default + per-contact) | [src/lib/components/contact-cadence-fields.svelte](mdc:src/lib/components/contact-cadence-fields.svelte) |
| Contacts Lists tab panel (list CRUD, mass-add checklist, default cadence, Log cards sent) | [src/lib/components/contacts-lists-panel.svelte](mdc:src/lib/components/contacts-lists-panel.svelte) |
| Lists-tab multi-select add checklist (household-first; Not on list / Has address / All) | [src/lib/components/contact-list-add-panel.svelte](mdc:src/lib/components/contact-list-add-panel.svelte) |
| Sheet list membership checkboxes (`member_list_id`) | [src/lib/components/contact-list-toggles.svelte](mdc:src/lib/components/contact-list-toggles.svelte) |
| Household create/edit Sheet (envelope name + structured address + list toggles) | [src/lib/components/household-form-sheet.svelte](mdc:src/lib/components/household-form-sheet.svelte) |
| Detailed Log Contact dialog (note + optional backdate; kind=meet) | [src/lib/components/log-contact-dialog.svelte](mdc:src/lib/components/log-contact-dialog.svelte) |
| Detailed Log cards sent dialog (list-wide kind=card fan-out; note + date) | [src/lib/components/log-cards-dialog.svelte](mdc:src/lib/components/log-cards-dialog.svelte) |
| Assignment create/edit Sheet (course, title, kind, status, due_date, same-course parent, notes; edit mode + kind=paper shows "Open research paper" sibling form — [189](mdc:docs/decisions/189-classwork-papers-session-1.md)) | [src/lib/components/assignment-form-sheet.svelte](mdc:src/lib/components/assignment-form-sheet.svelte) |
| Paper create/edit Sheet (title, status, assignment link with 1:1 guard, course/due locked while linked — P1 stamp+lock, topic, passage, notes) | [src/lib/components/paper-form-sheet.svelte](mdc:src/lib/components/paper-form-sheet.svelte) |
| Paper source row — Turabian Footnote/Short/Bib copy + page input, per-source research note editor, Not-owned badge, remove callback, research-group select (auto-submit; hidden when paper has no groups) | [src/lib/components/paper-source-row.svelte](mdc:src/lib/components/paper-source-row.svelte) |
| Paper add-source panel — `?src_q=` catalog search (books + essays), attach forms with Attached dedupe, free-form not-owned stub create+attach | [src/lib/components/paper-add-source-panel.svelte](mdc:src/lib/components/paper-add-source-panel.svelte) |
| Paper research-group section header — name + count, inline rename form, move up/down callbacks (ordered-id reorder), delete callback (confirm owned by host page) | [src/lib/components/paper-group-header.svelte](mdc:src/lib/components/paper-group-header.svelte) |
| Classwork Assignments \| Papers view toggle (shared by `/classwork` + `/classwork/papers`) | [src/lib/components/classwork-view-toggle.svelte](mdc:src/lib/components/classwork-view-toggle.svelte) |
| Sermons List \| By book view toggle (shared by `/sermons` + `/sermons/by-book`) | [src/lib/components/sermons-view-toggle.svelte](mdc:src/lib/components/sermons-view-toggle.svelte) |
| Project links inline editor (metadata Sheet, edit mode) | [src/lib/components/project-links-editor.svelte](mdc:src/lib/components/project-links-editor.svelte) |
| Projects health/lifecycle visual tokens (Epic hex + picker segment classes, lifecycle badge) | [src/lib/projects/health-appearance.ts](mdc:src/lib/projects/health-appearance.ts) |
| Domain color palette keys + static Tailwind class maps | [src/lib/projects/project-colors.ts](mdc:src/lib/projects/project-colors.ts) |

shadcn primitives (Button, Input, Label, Select, Sheet, Dialog, Badge, Separator) live under `src/lib/components/ui/`. The `Button` accepts a `hotkey` prop (Cmd+letter from the reserved set `s u d e g b`, or the special string `Escape` for Cancel) and an optional `label` prop; together they wire the chord, an underlined-letter label, and a tooltip. **Cancel buttons use `hotkey="Escape"`** (bare Esc; bubble phase, defaults-prevented bail). Anchors (`href`) skip the dev-warn but can still take a hotkey. Full convention: [.cursor/rules/hotkeys.mdc](mdc:.cursor/rules/hotkeys.mdc).

## Patterns

- **Sheet vs Dialog vs dedicated page**: small/medium forms (≤~10 fields) → `Sheet` (full-screen on mobile, drawer on desktop). Confirmations and small pickers → `Dialog`. **Large forms (~20+ fields with junctions)** → dedicated page route (`/foo/new`, `/foo/[id]/edit`) with a presentational `<XForm>` component shared by both. The right-side drawer caps at `max-w-2xl` and gets cramped past about 10 fields; pages can use 2-col desktop layouts and breathe. See `book-form.svelte` + `/library/books/new` + `/library/books/[id]/edit` for the canonical full-page pattern.
- **Create + edit share one form/sheet**: take an optional `entity` prop; "create" mode = `entity == null`. See `time-entry-sheet.svelte` (sheet) and `book-form.svelte` (page-hosted).
- **Bindable props for v-model-style state**: `let { open = $bindable(false), value = $bindable<T[]>([]) } = $props();`
- **Form actions inside components**: use `use:enhance` and call back to the page via `onSaved` / `onSuccess` / `onError` callback props. For full-page forms also expose `onDirtyChange` so the host page can wire `beforeNavigate` for unsaved-edit confirmation.
- **Svelte 5 runes**: `$state`, `$props`, `$bindable`, `$derived`, `$effect`. Do not use Svelte 4 reactive `$:` syntax.
- **Heavy route-local sub-forms (bundle)**: if a child `.svelte` is large (bulk editors, uploads, barcode) but only mounts after a click or `bind:open`, **do not statically import it from the page** — use `import()` + a `$state` constructor holder (or `{#await import(...)}` inside the open gate). For `$effect`-driven preloads, gate with `browser` from `$app/environment` so SSR does not execute client-only dynamic imports. Reference: [025](mdc:docs/decisions/025-library-bundle-split.md), `/library/books/[id]/+page.svelte`, `book-form.svelte` + OL dialog.

## When to add a new component

- The pattern doesn't match any row in the inventory above
- A `.svelte` file exceeds ~150 lines
- The same JSX appears in 2+ pages

When you add one, update this rule.
