---
description: Library module conventions — polymorphic FKs, viewer-write, search-first
globs: src/routes/library/**,src/routes/settings/library/**,supabase/migrations/**library**
alwaysApply: false
---

# Library module rules

The library module differs structurally from invoicing. Build with these defaults.

## Polymorphic `(book_id OR essay_id)` is everywhere

`scripture_references`, `book_topics`, `book_bible_coverage`, `book_ancient_coverage` all have a CHECK enforcing exactly one of `book_id` / `essay_id`.

- TypeScript: model as a discriminated union, never as `{ book_id?: string; essay_id?: string }`.

  ```ts
  type Source = { kind: 'book'; bookId: string } | { kind: 'essay'; essayId: string };
  ```

- UI: build **one** `<SourcePicker>` component and reuse across all four tables. Do not invent four versions.

## Viewer has write access (unlike invoicing)

The `viewer` role can `INSERT` and `UPDATE` on `books`, `scripture_references`, `book_topics`, `people`, `series`, and the coverage/junction tables. RLS uses `app_is_viewer_writer('library')`.

Every library session must include a "viewer can / cannot do X" acceptance line. Seed a viewer user and the corresponding `user_permissions` row in Session 0; do not defer.

## Search is the primary verb

Library is a search module, not a CRUD module. The scripture overlap query is the central UX:

```sql
WHERE bible_book = ?
  AND verse_start_abs <= ?  -- search range end as (chapter*1000 + verse)
  AND verse_end_abs >= ?    -- search range start
```

`verse_start_abs` / `verse_end_abs` are computed by trigger. Never compute them in app code.

A working search UI on seed data should land by Session 2 or 3, not at the end.

## Canonicalization patterns

Two surfaces share the same shape — build one component, use twice:

- `ancient_texts.canonical_name` + `abbreviations[]` with fuzzy "Did you mean X?" before insert
- `book_topics.topic` (`CHECK (topic = lower(trim(topic)))`) with autocomplete against existing topics

Component name: `<CanonicalizingCombobox>` (or similar). Inputs: existing values, fuzzy match function, "create new" callback.

## OCR / image ingest pipeline

`scripture_references` has `source_image_url`, `confidence_score`, `needs_review`, `review_note` — clearly anticipating an image-ingest pipeline. **Decide explicitly per session**: either build it (image upload → Edge Function OCR → staging rows with `needs_review = true`) or defer it and require manual entry. Do not leave ambiguous.

## Genre + language enums

`books.genre` is enforced by `books_genre_check` (see migration `20260507190000`) and must match `GENRES` in `src/lib/types/library.ts`. `books.language` is constrained by a CHECK. Treat as Settings concerns or named constants in `src/lib/types/library.ts` — do not improvise free-text lists in forms.

## Patterns landed in Session 1

- **Diff-based junction sync.** `syncAuthors` / `syncCategories` in `src/lib/library/server/book-actions.ts` compute insert / update / delete sets relative to current rows. Audit log shows minimal diffs. Reuse this shape for any future junction with sort_order or extra columns.
- **Inline create-via-fetch.** `submitPersonDialog` in `book-form.svelte` shows the pattern for creating a sub-entity mid-form-fill: POST to `?/createPerson` via `fetch`, parse the action response with `deserialize` from `$app/forms`, then mutate local state. Pattern reuses for any "+ New X" affordance inside another form.
- **Inline B14 dedup hint** is a yellow caption beneath the picker once a person is selected; it is the Session 1 stand-in for the full `<CanonicalizingCombobox>` (Session 5).
- **Action duplication via shared helpers.** Each route hosts only the form actions it needs (e.g. `softDeleteBook` lives on the list page AND the detail page; `createBook` lives on `/library/books/new`; `updateBook` lives on `/library/books/[id]/edit`). The implementations live in `src/lib/library/server/book-actions.ts`; the per-route handlers are 4-line wrappers. Avoid duplicating an action onto a route that doesn't surface it (e.g. don't put `createBook` on the list page just because the old sheet trigger lived there).
- **Pre-regen migration columns.** When a delta migration is staged but unapplied:
  - **Never** name the new columns in supabase-js SELECT lists. PostgREST 42703s on absent columns, and `as never` casts only protect TypeScript, not runtime.
  - **Conditionally** include new columns in INSERT/UPDATE payloads only when the value is populated. `bookColumnsPayload` and `createPersonAction` show the pattern.
  - **Tag** with `// TODO post-delta-vN` so the cleanup is greppable after `npm run supabase:gen-types` runs.
  - **Cross-check** loaders against `00000000000000_baseline.sql` (and any later applied migrations), not against the schema doc — Session 1 lost time to `series.deleted_at` / `people.deleted_at` not actually being in baseline despite the doc claiming the convention.
- **Soft-delete UX = 10s undo toast.** Soft-delete fires immediately, toast offers Undo for 10s, otherwise redirect to list with `?deleted=<id>` so the list page can surface a follow-up banner. Detail-page deletes must `redirect(303, '/library?deleted=<id>')` from the action handler — the load function returns null for soft-deleted rows and would 404 otherwise. Toast lives on the list page where it survives the redirect.

## Patterns landed in Session 1.5

- **Typeahead person picker** (`<PersonAutocomplete>`) — search-style replacement for the per-author Select. Live filter on `last_name + first_name + aliases[]`, top 8 matches, B14 dedup warning shown inline per result, "+ Create '<typed>'" affordance when no exact match. This is the prototype for the `<CanonicalizingCombobox>` shipping in Session 5; same shape (bind:value, items prop, onCreate(rawText), keyboard nav).
- **Pre-flight B14 collision warning** — before creating a new person inside the inline dialog, scan `people` for `last_name + first_initial` matches and surface a yellow "Already in your library: …. Continue?" pattern. Re-arm the confirm flag whenever the user edits the name fields so a typo-corrected resubmit doesn't bypass a new collision.
- **Nested modals on top of `<Sheet>` or `<Dialog>`** — must use shadcn `<Dialog>` (or another portal-based primitive that stops event propagation). A hand-rolled `<div class="fixed">` will let outside-click handlers on the parent eat all in-progress form state.
- **Optimistic-UI inline edit** for single-column updates from a list/detail surface. The submit factory captures the user's intent into a local optimistic map; after `update({ reset: false })` the canonical state from the load function takes over. See `statusSubmit` in `src/routes/library/+page.svelte` for the pattern.
- **Soft-delete revert at the audit-log layer** — single-key patch (`deleted_at` flip), no whole-row replace. Add a table to `_SOFT_DELETE_REVERTIBLE_TABLES` in `src/routes/settings/audit-log/+page.server.ts` to enable it; the existing `_REVERTIBLE_TABLES` whitelist (whole-row replace) remains the more conservative path for non-soft-delete UPDATEs.
- **Audit-log entity name surfacing** — `entityLabelFor(table_name, ...)` extracts a friendly label from the JSONB payload per table (book title, person full name, scripture-ref formatted as `Bible Book ch:vs`, etc.). Junctions intentionally return null because `record_id` is already the parent UUID after the composite-PK fix.
- **Article-stripped sort key** — use [`src/lib/library/title-sort.ts`](mdc:src/lib/library/title-sort.ts) `titleSortKey(title, language)` (English + German articles when `language === 'german'`). List loaders call it after fetch; promote to a stored generated column if dataset crosses ~10k rows.
- **Dirty-form confirm via `beforeNavigate` (page-hosted forms)** — when the entity form lives on a dedicated page route (`/library/books/new`, `/library/books/[id]/edit`), use SvelteKit's `beforeNavigate` from `$app/navigation` instead of intercepting Sheet `onOpenChange`. The form component snapshots field state into `initialSnapshot`, derives `dirty = currentFormSnapshot() !== initialSnapshot`, and exposes `onDirtyChange(d)` so the host page can `cancel()` and show a `<ConfirmDialog>` ("Discard / Keep editing") for any dismiss path (back button, breadcrumb click, link click). After a successful save the form must re-baseline `initialSnapshot = currentFormSnapshot()` so the success-driven `goto()` doesn't re-trigger the prompt. The host page sets a `confirmedDiscard = true` flag before the post-save `goto()` for the same reason.
  - **CRITICAL: wrap any state read inside the seed/init `$effect` body in `untrack(() => …)`** from `svelte`. Svelte 5 `$effect` tracks every state read in its body — including helper-function calls (`currentFormSnapshot()`) AND probe-style reads like `if (authorRows.length === 0)`. Anything tracked re-fires the effect, which re-pre-fills fields (silently wiping user edits) and re-baselines the snapshot (silently flipping dirty back to false), which can in turn confuse a page-level `beforeNavigate` interceptor and produce "URL updates but page doesn't" stuck navigation. The `dirty = $derived(...)` SHOULD recompute on field changes — that's the point of $derived — but the seed effect must NOT. Both the snapshot capture (Session 1.5c) and the seed-if-empty length check (Session 1.5h) had to be wrapped in `untrack` for this reason.
  - See [`src/lib/components/book-form.svelte`](mdc:src/lib/components/book-form.svelte) + [`src/routes/library/books/new/+page.svelte`](mdc:src/routes/library/books/new/+page.svelte) for the canonical implementation.
  - For sheet-hosted forms (smaller; e.g. `<TimeEntrySheet>`) the controlled-Sheet `onOpenChange` interceptor is still the right pattern — `beforeNavigate` doesn't fire on outside-click dismissal of a sheet.
- **Large entity forms ship as dedicated pages, not sheets** — once a form crosses ~10-15 fields with junctions, the `max-w-2xl` sheet width gets cramped and breaks dropdown layouts (truncated select labels, wrapping autocomplete results, overlay buttons colliding with input placeholders). Promote to `/foo/new` + `/foo/[id]/edit` page routes with a presentational `<XForm>` component, give the page wrapper `max-w-5xl` (or wider) and a 2-column desktop layout for less-critical sections. The list/detail "Add" + "Edit" affordances become `<a>` links instead of sheet triggers.
- **Auto-flag-for-review at the server-side parser** — for entities where every field is optional (per the "save anything, flag incomplete" model on books), compute `missingImportant` from a fixed list of citation-critical fields, force `needs_review = true` when any are missing, and merge a "Missing: X, Y" auto-line into `needs_review_note` (replacing if existing note matches `/^Missing:\s/`, otherwise prepending + blank line + existing note to preserve user-authored notes). Export the `computeMissingImportant` helper so the form can show a matching pre-save preview hint. See `parseBookForm` + `computeMissingImportant` in [`src/lib/library/server/book-actions.ts`](mdc:src/lib/library/server/book-actions.ts).
- **`hasAnyField` save bar** — for entity forms where every individual field is optional, require at least ONE non-default value at parse time before allowing INSERT. Avoids accidental empty rows without forcing any specific field. Mirror the check on the form's Save-button disable + render a "Add at least one detail before saving" hint when disabled. Default-valued fields (language='english', reading_status='unread') don't count — they're always present.

## Scripture reference semantics

`compute_verse_abs()` (`00000000000000_baseline.sql` + corrected in `20260429180000_compute_verse_abs_chapter_only_fix.sql`) implements:

- **whole book** (`chapter_start IS NULL`) → `verse_start_abs = 0`, `verse_end_abs = 999999`
- **chapter only** (`chapter_start` set, no `verse_start`, no `chapter_end`) → `cs*1000 .. cs*1000+999`
- **chapter range** (`chapter_start` + `chapter_end`, no verses) → `cs*1000 .. ce*1000+999`
- **verse only** (`chapter_start` + `verse_start`, no `chapter_end`) → `cs*1000+vs .. cs*1000+vs`
- **verse range** (all four set) → `cs*1000+vs .. ce*1000+ve`

"Open-ended from chapter to end-of-book" is intentionally NOT supported by null overloading — set `chapter_end` to the actual last chapter. If real workflows demand open-ended semantics, add a `scope_kind` column rather than further overloading null.

When forms collect chapter/verse, treat blanks as "leave null"; the trigger does the rest. Never set `verse_*_abs` from app code.

## Bulk entry forms

For sub-entities where users typically add many rows at once (scripture references, eventually book topics + ancient coverage), a single component can drive both single-edit and bulk-create:

- Edit mode: pass `existingRef` → render one row, hide Add/Duplicate/Remove controls, post legacy single-row contract to `?/updateXxx`.
- Batch create mode: render N draft rows starting with one, show Add/Duplicate/Remove, post a `rows_json` JSON array to `?/createXxxsBatch`.
- Server: shared row-level parser; the batch parser silently skips rows where the user-relevant required fields are all blank (avoids "trailing empty row" friction).
- Batch insert is one `.insert(rows)` call so the audit trigger logs one row per child entity but the round-trip is single. Pre-validate every row in the parser; relying on a CHECK violation to surface partial failure is cheaper to avoid than to handle.
- Image / shared-resource uploads sit at the top of the batch UI when one resource (a page photo) attaches to many rows. Per-row image override is a separate, later decision.

See [`src/lib/components/scripture-reference-form.svelte`](mdc:src/lib/components/scripture-reference-form.svelte) and [`src/lib/library/server/scripture-actions.ts`](mdc:src/lib/library/server/scripture-actions.ts) for the canonical implementation.

## Storage buckets (Session 2)

When an entity needs image attachments (scripture page scans today; book covers, ancient-text page snippets, etc. tomorrow), follow this shape:

- **Naming**: `${module}-${entity}-images`, lowercase + dashes, plural entity. First example: `library-scripture-images`.
- **Visibility**: private. Reads are always via signed URL (1h TTL per Tracker_1 S8) generated **server-side** in the loader — never store a signed URL in the row, store the bucket object PATH only. Signed URLs expire; the path doesn't.
- **Path convention**: `${userId}/${parentId}/${random}.${ext}` — `userId` first because storage RLS enforces self-prefix INSERT via `(storage.foldername(name))[1] = auth.uid()::text`. `parentId` (e.g. `bookId`) is informational; row-level RLS on the parent table provides the real read isolation. Build paths with a single helper alongside the loader so server + client share the truth (see `src/lib/library/storage.ts` — `scriptureImagePath(...)`).
- **RLS shape on `storage.objects`** (filter by `bucket_id = '<bucket>'`):
  - SELECT: `app_is_owner() OR app_is_viewer_writer('<module>')` — anyone with module read access
  - INSERT: `(app_is_owner() OR app_is_viewer_writer('<module>'))` AND first path segment matches `auth.uid()::text` — viewer-write but only into their own prefix
  - UPDATE: `app_is_owner()` — defensive; viewers don't rewrite each other's files
  - DELETE: `app_is_owner()` — same reason
- **Migration warning**: `COMMENT ON POLICY` on `storage.*` requires ownership of the table that the migration role doesn't have (SQLSTATE 42501). Skip COMMENTs in storage migrations and put policy semantics in the file's header block instead. See `supabase/migrations/20260428200000_library_scripture_images_bucket.sql`.
- **Upload UX**: do uploads from the **browser supabase client** (form component) — don't stream files through SvelteKit form actions. The form posts only the resulting object PATH (text). Client-side downscale (~2048px long edge / JPEG q=0.85 via `createImageBitmap` + `<canvas>`) before upload; fall back to uploading the original if `createImageBitmap` throws (HEIC on Safari can do this). 10 MB bucket cap is the upper backstop.
- **Loader signs URLs in parallel** for any row that has a non-null path; null path → null URL. The view-model carries both fields (`*_url` for the path, `*_signed_url` for the rendered link).

## Patterns landed in Session 3

- **URL-param-as-source-of-truth for list filters.** Server `load` parses `url.searchParams` into a typed `Filters` shape (validate enums against the closed lists in `$lib/types/library`); the page renders against `data.filters` (NOT a local mirror); user toggles call a `pushFilters(next)` helper that builds a new URL and `goto(target, { keepFocus: true, noScroll: true })`. Back/forward, deep-linking, and SSR-safety fall out for free. The `multiParam(url, key)` helper in [`src/routes/library/+page.server.ts`](mdc:src/routes/library/+page.server.ts) accepts BOTH repeated `?genre=A&genre=B` AND CSV `?genre=A,B` so URLs paste cleanly; emit only the repeated form on round-trip. Reuse for any list page that needs faceted filtering.
- **Snippet-as-single-source-of-truth for desktop-aside + mobile-sheet panels.** When the same chip-rail / filter form must render in two layout slots (sticky aside on desktop, `<Sheet side="bottom">` on mobile), define `{#snippet panelBody()}` once at module scope and `{@render panelBody()}` in both slots. Beats extracting a third component for markup that's wholly co-owned by a single page. See [`src/routes/library/+page.svelte`](mdc:src/routes/library/+page.svelte) `{#snippet filterBody()}`.
  - **Snippets MUST live in markup, not script.** They sit between `</script>` and the page's outer markup, NOT inside the script tag. The compiler's "`</script>` attempted to close an element that was not open" error means a snippet got placed in the wrong half of the file.
- **Hash-driven deep-link highlight with auto-clear.** `$effect` tracks `page.url.hash` (from `$app/state`), matches a typed pattern (`^#ref-<uuid>$`), `scrollIntoView`s the matched element, sets a state flag for ~2s, then clears. Tracking the hash means back/forward navigation (which doesn't trigger a fresh `load`) re-runs the effect cleanly. Reuse for any list page that needs linkable sub-rows. See [`src/routes/library/books/[id]/+page.svelte`](mdc:src/routes/library/books/%5Bid%5D/+page.svelte) `highlightedRefId`.
- **PostgREST `.or()` across joined tables via parallel resolve + `id.in.()`.** When a single `q` keyword needs to match columns on `books` AND a join (e.g. `people.last_name` via `book_authors`), don't reach for a SECURITY INVOKER SQL function as the first move. Instead: parallel SELECT on the joined table to get the list of matching FKs, then append `id.in.(<csv>)` to the same `.or()` clause. One `books` round-trip, no SQL function needed. Pivot to a SQL function only if the FK list grows into kilobytes of URL. See `loadBookListFiltered` in [`src/lib/library/server/loaders.ts`](mdc:src/lib/library/server/loaders.ts).
- **`pg_trgm` GIN indexes on substring-search targets.** Any column that gets `ILIKE '%q%'` from a list-page keyword search needs `gin (col gin_trgm_ops)` — a B-tree index won't help substring matching. Install once at the module level with `CREATE EXTENSION IF NOT EXISTS pg_trgm`; the typegen output will pick up `show_limit` / `show_trgm` helper functions in the public schema (expected; commit alongside the migration).

## Patterns landed in Session 5.5

- **`needs_review = false` override on explicit-user-reviewed flows.** The auto-flag-for-review contract from Session 1.5c is one-way — `parseBookForm` recomputes `missingImportant` on every save and snaps `needs_review` back to `true` when fields are missing. The `/library/review` queue introduces the inverse: an explicit-user-reviewed Save in `reviewSaveAction` flips `needs_review = false` regardless of remaining gaps, and **strips** the `Missing: …` auto-line via the new `stripReviewAutoLine` helper (sibling to `mergeReviewNote` in [`src/lib/library/server/book-actions.ts`](mdc:src/lib/library/server/book-actions.ts)). This is the right escape hatch for "I know it's incomplete; stop flagging it." `stripReviewAutoLine` preserves any user-authored note portion verbatim. Reuse this shape for any future "I have manually reviewed this" surface (e.g. low-confidence OCR rows in Session 9).
- **Card-stack pattern with JSON refill endpoint** (`/library/review` + `/library/review/queue/+server.ts`). Page server load returns the first N cards (10); the page tracks `excludedIds: Set<string>` in `$state` and refills via `fetch('/library/review/queue?exclude=...&limit=10')` when the local stack drops below 3. URL stays clean except for filter params — encoding the skipped set in URL bloats every navigation. Reuses for any "drain a queue one card at a time" surface where ordering doesn't matter and skipped-this-session shouldn't replay.
- **Shared URL parsers for a route + sibling `+server.ts` live in `$lib`.** Do not export helpers from [`src/routes/library/review/+page.server.ts`](mdc:src/routes/library/review/+page.server.ts); keep that module to `load` / `actions` (and Kit file exports) only so tooling and deployments do not treat route files as arbitrary libraries. `parseReviewFilters` lives in [`src/lib/library/review.ts`](mdc:src/lib/library/review.ts) and is imported by [`src/routes/library/review/+page.server.ts`](mdc:src/routes/library/review/+page.server.ts), [`src/routes/library/review/queue/+server.ts`](mdc:src/routes/library/review/queue/+server.ts), and [`src/routes/library/+page.server.ts`](mdc:src/routes/library/+page.server.ts) (same filter shape for the `/library/review` link).
- **PostgREST 16KB header cap caps `.in('uuid_col', slice)` at ~200 entries.** A 36-char UUID + comma at PAGE=500 produces ~18KB URLs and undici raises `HeadersOverflowError`. The Pass-1 importer's existing `loadAuthorsForBooks` / `loadFirstAuthorLastNames` paths slice at 500 because they currently see < 200 unique ids; **lower to 200 if a future bulk job widens the in-list**. The new `patch-import-match-type.ts` uses `PAGE = 200` directly. Same constraint applies to `id.in.(...)` filters built into PostgREST `.or()` strings (Session 3) — pivot to a SQL function once the id list crosses ~150-200 entries.

## Patterns landed in Session 7 (people)

- **`/settings/library`** — overview hub (`+page.svelte`); tabs: Overview, People, Merge (owner-only), Series, Ancient texts, Genres, Categories, Bible books; `isOwner` from [`+layout.server.ts`](mdc:src/routes/settings/library/+layout.server.ts).
- **`/settings/library/people`** — list, search, edit, soft-delete, per-row merge dialog; server actions in [`people-settings-actions.ts`](mdc:src/lib/library/server/people-settings-actions.ts); `depends('app:library:people')` + `invalidate('app:library:people')` on mutations.
- **`/settings/library/people/merge`** — B14 suggested pairs, dismiss (`localStorage` key `ppp.library.mergeDismissedPairs`), manual two-select merge; **`fetchLiveBookIdsByPersonId`** in [`people-settings-book-counts.ts`](mdc:src/lib/library/server/people-settings-book-counts.ts) for chunked live book counts (join non-deleted `books`).

## Patterns landed in Session 7b (settings + permissions)

- **`/settings/library/series`** — [`series-settings-actions.ts`](mdc:src/lib/library/server/series-settings-actions.ts); `depends('app:library:series')` on `/library/books/new` + edit loads; invalidate after mutations.
- **`/settings/library/ancient-texts`** — CRUD + merge (typed confirm of canonical name); RPC `library_merge_ancient_texts`; `depends('app:library:ancient_texts')` on book detail load.
- **Viewer library access** — SQL helpers `app_module_access_level` / `app_has_module_read` gate library `SELECT`; read-only viewers may `UPDATE` `books.reading_status` only (BEFORE UPDATE trigger). `/settings/permissions` (owner) edits `user_permissions`.
